[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "larpcheck-dev"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "cryptography",
    "tkinterdnd2",
    "tkinterweb",
]

[tool.uv]
package = false
